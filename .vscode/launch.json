{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug executable 'json_typings'",
            "cargo": {
                "args": [
                    "build",
                    "--bin=main",
                    "--package=json_typings"
                ]
            },
            "args": ["./data/sample_b.json"],
            "cwd": "${workspaceFolder}"
        }
    ]
}